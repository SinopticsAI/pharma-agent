{
  "name": "pharma-agent",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mastra intake agents for the MedMost cabinet. Drafts only: never files, never signs, never spends.",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "mastra dev",
    "build": "mastra build",
    "start": "node .mastra/output/index.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/mastra/extract.test.ts src/mastra/tools/jsonish.test.ts"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^1.0.0",
    "@mastra/ai-sdk": "^1.10.0",
    "@mastra/core": "^1.63.2",
    "@mastra/memory": "^1.28.1",
    "@mastra/pg": "^1.22.2",
    "@napi-rs/canvas": "^1.0.8",
    "unpdf": "^1.8.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "mastra": "^1.27.2",
    "typescript": "^5.6.0"
  }
}
